defaults:
  - finetune # Inherits base training arguments like learning_rate, batch_size, etc.

handler: GeometricUnlearn

method_args:
  geometric_config:
    ema_momentum: 0.99
    mu: 1.0
    wasserstein_epsilon: 0.1
    ot_smoothing_eta: 0.05
    lambda_gp: 0.5
    gp_target: 1.0
    gp_topk: 8